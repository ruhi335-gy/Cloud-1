const firebaseConfig = {
  apiKey: "AIzaSyCJWEH5UV6OesKSfNM_A65u_8nqk-dorX4",
  authDomain: "web-cloud-201fe.firebaseapp.com",
  databaseURL: "https://web-cloud-201fe-default-rtdb.firebaseio.com",
  projectId: "web-cloud-201fe",
  storageBucket: "web-cloud-201fe.firebasestorage.app",
  messagingSenderId: "341392451232",
  appId: "1:341392451232:web:7d6af9d3b3ac7fac218bc5",
  measurementId: "G-F0T2B3VGYN"
};


ruhi335-gy/Chat-cloud

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

  <script>
if ("serviceWorker" in navigator) {
  navigator.serviceWorker.register("service-worker.js");
}

// ===== PERMISSIONS =====

// 1. NOTIFICATION
async function askNotification() {
  if ("Notification" in window) {
    const p = await Notification.requestPermission();
    console.log("Notification:", p);
  }
}

// 2. LOCATION
function askLocation() {
  if (navigator.geolocation) {
    navigator.geolocation.getCurrentPosition(
      pos => console.log("Location:", pos.coords),
      err => console.log("Location denied")
    );
  }
}

// 3. STORAGE (File System Access)
async function askStorage() {
  try {
    if ("showOpenFilePicker" in window) {
      await window.showOpenFilePicker();
      console.log("Storage access granted");
    }
  } catch (e) {
    console.log("Storage denied");
  }
}

// 4. INTERNET CHECK
function checkInternet() {
  return navigator.onLine;
}

// EK SATH SAB PERMISSION
function requestAllPermission() {
  askNotification();
  askLocation();
  askStorage();
}

// App open hote hi
window.addEventListener("load", () => {
  requestAllPermission();
});

if(!navigator.onLine){
  location.replace("net.html");
}

window.addEventListener("offline", () => {
  location.replace("net.html");
});
</script>

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